import { momentum } from "./momentum";
import { risk } from "./risk";
import { general } from "./general";
import { reward } from "./reward";

export const query: { [key: string]: string } = {
  momentum,
  risk,
  general,
  reward,
};
